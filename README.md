<!-- Dependencies: -->

npm install typescript --save-dev  <!-- Instalar o typescript -->
tsc -init  <!-- Iniciar o typescript -->
npm install fastity @fastify/cors  <!-- Instalar o fastify -->
npm install tsx  <!-- Instalar o tsx -->
npm install @types/node --save-dev  <!-- Instalar os types -->